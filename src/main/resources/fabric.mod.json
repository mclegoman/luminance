{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${version}",
  "name": "${modName}",
  "description": "${modDescription}",
  "authors": [
    "${authors}"
  ],
  "contributors": [
    "${contributors}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "${issues}",
    "sources": "${sources}"
  },
  "license": "${license}",
  "icon": "assets/${modId}/icons/icon.png",
  "depends": {
    "minecraft": ">=${mc}",
    "fabric-api": ">=${fapi}"
  },
  "entrypoints": {
    "client": [
      "com.mclegoman.${modId}.client.LuminanceClient"
    ],
    "modmenu": [
      "com.mclegoman.${modId}.compat.ModMenuCompat"
    ]
  },
  "custom": {
    "modmenu": {
      "badges": [
        "library"
      ]
    }
  },
  "mixins": [
    "dt-${modId}.mixins.json"
  ]
}